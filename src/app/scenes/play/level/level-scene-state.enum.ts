/**
 * Level scene state enumeration.
 */
export enum LevelSceneState {
  /**
   * Init.
   */
  Init = 'Init',

  /**
   * Start.
   */
  Start = 'Start',

  /**
   * Finish.
   */
  Finish = 'Finish'
}
