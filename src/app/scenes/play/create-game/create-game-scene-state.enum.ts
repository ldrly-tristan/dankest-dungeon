/**
 * Create game scene state enumeration.
 */
export enum CreateGameSceneState {
  /**
   * Init.
   */
  Init = 'Init',

  /**
   * Generate level.
   */
  GenerateLevel = 'GenerateLevel',

  /**
   * Finish.
   */
  Finish = 'Finish'
}
