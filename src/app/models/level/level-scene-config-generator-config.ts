/**
 * Level scene configuration generator configuration.
 */
export interface LevelSceneConfigGeneratorConfig {
  /**
   * Id.
   */
  id: string;

  /**
   * Seed.
   */
  seed: string;

  /**
   * Width in cells.
   */
  width: number;

  /**
   * Height in cells.
   */
  height: number;
}
